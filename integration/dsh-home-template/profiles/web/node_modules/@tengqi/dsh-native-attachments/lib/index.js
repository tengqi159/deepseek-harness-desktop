/**
 * Host half of the native-attachment surface. The 0.1.1-rc.2 client-module scanner
 * discovers ./client through package.json#dsh.client; no host behavior is
 * required and upstream DeepSeek Harness remains untouched.
 */
function apply() {}

export { apply };
