/**
 * Host half of the capability catalog. The 0.1.1-rc.2 client-module scanner finds
 * ./client from package.json; no host behavior or upstream patch is required.
 */
function apply() {}

export { apply };
