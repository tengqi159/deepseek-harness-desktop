/**
 * Host half of the capability catalog. The rc.6 client-module scanner finds
 * ./client from package.json; no host behavior or upstream patch is required.
 */
function apply() {}

export { apply };
